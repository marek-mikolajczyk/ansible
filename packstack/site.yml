- hosts: green.marekexample.com
  user: root
  gather_facts: false
  tasks:

    - import_role:
        name: ovirt_vm

- hosts: packstack.marekexample.com
  user: root
  gather_facts: false
  tasks:

    - import_role:
        name: marekexample_packstack

    - import_role:
        name: packstack
